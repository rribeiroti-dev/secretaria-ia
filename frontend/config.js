/**
 * Configuração de ambiente do frontend.
 * Em produção, troque API_BASE_URL pela URL pública do backend no Render
 * (ex.: "https://secretaria-particular-api.onrender.com").
 * Nenhum segredo deve ser colocado neste arquivo — ele é público.
 */
window.APP_CONFIG = {
  API_BASE_URL: "http://localhost:8000",
};
