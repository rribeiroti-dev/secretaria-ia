fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0
python-dotenv==1.0.1

# Auth / segurança
pyjwt==2.10.1
passlib[argon2]==1.7.4
argon2-cffi==23.1.0
pyotp==2.9.0
cryptography==44.0.0
slowapi==0.1.9

# Banco de dados
supabase==2.11.0

# HTTP / integração com provedores de IA
httpx==0.28.1

# Embeddings locais (memória vetorial) — ONNX Runtime, sem PyTorch (leve o suficiente para o free tier do Render)
fastembed==0.8.0

# Upload multipart
python-multipart==0.0.20

# Testes
pytest==8.3.4
pytest-asyncio==0.25.0
